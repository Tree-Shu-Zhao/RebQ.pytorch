NAME: upmc_food101_cmml
DATA_DIR: ${DATA_DIR}/UPMC-Food101-CMML
MULTI_LABEL: False
PROCESSOR: ${model.NAME}

TRAIN_BATCH_SIZE: ${train.BATCH_SIZE}
TEST_BATCH_SIZE: ${test.BATCH_SIZE}
NUM_WORKERS: 8
LIMITED_TRAIN_DATA_RATIO: null
LIMITED_VAL_DATA_RATIO: null
MAX_LENGTH: 512

NUM_TASKS: 10
TOTAL_LABELS: 100
NUM_LABELS_PER_TASK: 10
missing_params:
  RATIO: 0.7
  TYPE: "both"
  BOTH_RATIO: 0.5
  SAVE_ROOT: ${data.DATA_DIR}/missing_tables